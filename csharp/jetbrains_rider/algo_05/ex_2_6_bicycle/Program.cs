using System;

namespace ex_2_6_bicycle
{
    class Program
    {
        static void Main(string[] args)
        {
            bool weatherIsGood;
            bool bicycleIsGoodShape;
            bool repairsAreFast;
            bool doIOwnGameOfThrones;
            bool libraryOwnsGameOfThrones;
            
            // If the weather is good
                // If the bicycle is in good shape
                    // I will ride my bike
                // But if bicycle is not in good shape
                    // If repairs are fast
                        // I will ride my bike
                    // Else
                        // I will walk
            // Else if the weather is not good, i will read the book Game of Thrones
                // If I have the book
                    // I take the book GOT
                // Else I haven't the book
                    // If the library owns the book
                        // I borrow GOT
                    // Else the library doesn't owns the book
                        // I borrow a detective story
                    // I go back home
                // I will sit and read the book
        }
    }
}